# ComputerUsageAgent

Agent that analyzes Windows Event Logs using MCP tools from a native C# server (`EventLogMcp`).

## Run

1. Start MCP server:
   - `cd ../EventLogMcp`
   - `dotnet run`
2. Run agent:
   - `cd .`
   - `dotnet run`

